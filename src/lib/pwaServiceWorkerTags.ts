/** Tags shared between the service worker and the app (must stay in sync). */
export const PWA_PERIODIC_SYNC_TAG = 'dsp-periodic-content';
export const PWA_BACKGROUND_SYNC_TAG = 'dsp-deferred-sync';
